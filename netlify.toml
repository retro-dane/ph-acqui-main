[build]
  publish = "public"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  GATSBY_CPU_COUNT = "1"
  # Disable AVIF processing to prevent build failures
  GATSBY_AVIF = "false"